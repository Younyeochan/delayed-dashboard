'use strict'

var nconf = require('nconf');
// console.log(nconf.get())

// nconf
//     .file('tscs', { file: __dirname + './tscs.json'});
    // .file('tscs', { file: __dirname + '/../config/tscs.json'});
    // .file('tscs', { file: __dirname + './config.js'});

module.exports = nconf;
